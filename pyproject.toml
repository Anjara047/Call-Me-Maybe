[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Function calling tool for LLMs"
requires-python = ">=3.10"

dependencies = [
    "numpy",
    "pydantic",
    "llm-sdk",
]

[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }
